Requirements:
-------------

To manage plugins I use vundle. To use vundle you have to have GIT installed.

Install vundle:
git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim

To install the plugins defined in the .vimrc open vim and type
:PluginInstall
